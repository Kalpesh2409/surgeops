const fs=require('fs');const content=fs.readFileSync('src/hooks/usePriceStream.ts','utf8');const fixed=content.replace('const es = new EventSource','fetch(http://localhost:4000/pricing/current/).then(r=>r.json()).then((data)=>{const map={};data.forEach((p)=>{map[p.productId]=p;});setPrices(map);setLastUpdated(new Date().toISOString());}).catch(()=>{});\n    const es = new EventSource');fs.writeFileSync('src/hooks/usePriceStream.ts',fixed);console.log(" done);